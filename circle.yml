machine:
  php:
    version: 5.4.6
    
dependencies:
  pre:
    - printf "\n" | pecl install -f memcache
    - composer config -g github-oauth.github.com 01a3f62f53abca01cf2267270ff7639b347c855a
    - composer install --optimize-autoloader
    
test:
  override:
    - phpunit
